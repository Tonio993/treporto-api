server:
  port: 8090

spring:
  datasource:
    url: jdbc:h2:tcp://localhost:9092/C:/db/treporto/test;AUTO_SERVER=TRUE
    username: sa
    password:
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true
    hibernate:
      ddl-auto: update
